# fly.toml — myshop-api

app = "myshop-api"
primary_region = "lhr"

[http_service]
  internal_port = 8080
  force_https = true

  # ALWAYS ON
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256
